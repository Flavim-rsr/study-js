// Aula 07 - Tipos de dados primitivos
// String, Number, undefined, boolean 

// String
const nome = "Flavio";
const nome1 = 'Flavio';
const nome2 = `Flavio`;

// Number
const num = 10;         // Numero inteiro
const num1 = 10.80;     // Numero de ponto flutuante

// Utilizar o null quando eu quero que ela em especificamente não aponte em local nenhum na memória
let nomeAluno;          // undefined = não aponta para local nenhum na memória
let sobrenome = null;   // Nulo = não aponta para local nenhum na memória

//Boolean (lógico)
const aprovado = true;      // Verdadeiro
const reprovou = false;     // Falso 

// Para verificar o tipo de qualquer variavel utilize o typeof no console.log()
console.log(typeof aprovado);
